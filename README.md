# Olimpiadas-Postal-de-Anuncios
Integrante 1 (Modelos, TDAs y Estructura): Morales Alex 
Integrante 2 (Lógica, CRUD, Recursividad y Validaciones): Oviedo Facundo 
Integrante 3 (Interfaz Web, Flask e Integración): Castillo Renzo

Prompt Inicial Integrante 1:

Actúa como un programador experto en Python y programación estructurada. Estoy participando en las olimpíadas de programación de la E.E.S.T. N° 6. Mi rol cubre parte del *30% de Lógica y Estructura de Código* y el *25% de Manejo de Datos y Persistencia* de la rúbrica.

Necesito que me ayudes a crear un archivo modelos.py para un *portal de anuncios de alquiler temporal* que incluya:

1. Un *Tipo de Dato Abstracto (TDA)* optimizado para la clase Apartamento (con atributos limpios: ID, título, ubicación, precio por noche y capacidad).

2. Una estructura de datos en memoria (lista/arreglo) robusta y bien inicializada con datos de prueba.

3. Código modular, sumamente limpio, sin repeticiones y con nombres claros, siguiendo estrictas buenas prácticas de programación estructurada.

⚠️ Contexto del grupo: El proyecto es una página web desarrollada en Python con Flask y estructurada en módulos (modelos.py, logica.py, crud.py, app.py). Por lo tanto, el código no debe usar la consola (input o print), sino ser funciones puras o clases que puedan ser importadas y llamadas fácilmente desde las rutas web de Flask.

Prompt Inicial Integrante 2: 

Actúa como un programador experto en Python. Estoy en las olimpíadas de programación y mi rol abarca el *Cumplimiento de Requerimientos Funcionales (20%)* y el *Manejo de Datos (25%)*.
> Necesito que me ayudes a escribir funciones en Python para un *portal de alquiler temporal* que cumplan exactamente con lo siguiente:

1. *Operaciones CRUD completas:* Alta, consulta, modificación y baja de apartamentos con integridad en los datos.

2. *Algoritmos de búsqueda, ordenamiento y filtrado* (por precio y ubicación).

3. *Al menos una solución recursiva aplicada a una funcionalidad real* (por ejemplo, el cálculo acumulativo o de descuentos de una reserva según la cantidad de noches).

4. *Validaciones estrictas de datos y control de excepciones:* El código debe prevenir drásticamente situaciones inválidas (precios negativos, campos vacíos o tipos incorrectos) para *evitar cualquier tipo de error fatal o crash* del sistema, asegurando robustez ante entradas maliciosas del usuario.

⚠️ Contexto del grupo: El proyecto es una página web desarrollada en Python con Flask y estructurada en módulos (modelos.py, logica.py, crud.py, app.py). Por lo tanto, el código no debe usar la consola (input o print), sino ser funciones puras o clases que puedan ser importadas y llamadas fácilmente desde las rutas web de Flask.

Prompt Inicial Integrante 3: 

Actúa como un desarrollador Full-Stack experto en Python usando *Flask* y tecnologías web ligeras. Mi rol en las olimpíadas cubre el *15% de Interfaz de Usuario y Experiencia (UX/UI)* y la integración del software.
Necesito que me ayudes a estructurar el archivo principal app.py con Flask y una interfaz web conectada a nuestros módulos de Python (modelos.py, logica.py, crud.py) asegurando:

1. Un *diseño intuitivo y visualmente atractivo* para el portal de anuncios de alquiler temporal.

2. *Mensajes de error claros en pantalla* cuando el usuario ingrese datos incorrectos o falle una validación.

3. Una integración fluida que permita mostrar catálogos, filtrar resultados y procesar formularios sin que la aplicación sufra cuelgues o interrupciones.

⚠️ Contexto del grupo: El proyecto es una página web desarrollada en Python con Flask y estructurada en módulos (modelos.py, logica.py, crud.py, app.py). Por lo tanto, el código no debe usar la consola (input o print), sino ser funciones puras o clases que puedan ser importadas y llamadas fácilmente desde las rutas web de Flask.
